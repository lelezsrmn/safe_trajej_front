import React, { useState } from 'react';

const Menu = () => {
    return (
        <div className="menu_container">
            <Link to='./screens/login'>Bouton</Link>
        </div>
    );
};

export default Menu;

